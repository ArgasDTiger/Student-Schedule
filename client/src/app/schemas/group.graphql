type Group {
  id: Int!
  groupNumber: Int!
  faculty: Faculty!
}

input GroupInput {
  id: Int!
  groupNumber: Int!
  faculty: FacultyInput!
}

input AddGroupInput {
  groupNumber: Int
  facultyId: Int
}

input UpdateGroupInput {
  id: Int!
  groupNumber: Int!
  facultyId: Int!
}
